<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <Property name="LOG_DIR">logs</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- Controllers -->
        <Logger name="io.github.lucaspaixaodev.poppin.infrastructure.input.rest"
                level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Database repositories -->
        <Logger name="io.github.lucaspaixaodev.poppin.infrastructure.output.database"
                level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Firebase gateway -->
        <Logger name="io.github.lucaspaixaodev.poppin.infrastructure.output.authentication"
                level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Spring & Hibernate — reduce noise -->
        <Logger name="org.springframework" level="INFO" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="org.hibernate" level="WARN" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Root level="INFO">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
